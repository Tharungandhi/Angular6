import { CollaborateSearchPipe } from './collaborate-search.pipe';

describe('CollaborateSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CollaborateSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
